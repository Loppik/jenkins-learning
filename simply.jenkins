def com1 = ""
pipeline {
  agent {
    label 'master'
  }
  triggers { pollSCM('* * * * *') }
  stages {
    stage('connect to remote server') {
      steps {
        echo "============= connect to remote server ============="
      }
    }
    stage('copy files') {
      steps {
        echo "============= copy files ============="
        sh '''
          ssh root@192.168.1.9 "cd /home/katrin/Projects/just_one_html && git pull origin dev"
        '''
      }
    }
    stage('reload server') {
      steps {
        echo "============= reload server ============="
        sh 'ssh root@192.168.1.9 service nginx reload'
      }
    }
  }
}
